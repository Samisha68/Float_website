# Float — Coming Soon

A minimal React, TypeScript, Tailwind CSS, and Vite landing page prepared for Vercel.

## Local development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

Vercel reads `vercel.json`, runs the production build, and serves the generated `dist` directory.
